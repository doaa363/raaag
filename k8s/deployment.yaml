apiVersion: apps/v1
kind: Deployment
metadata:
  name: logiCore-rag
  namespace: logiCore
  labels:
    app: logiCore-rag
spec:
  replicas: 2
  selector:
    matchLabels:
      app: logiCore-rag
  template:
    metadata:
      labels:
        app: logiCore-rag
    spec:
      containers:
        - name: rag-app
          image: logiCore/rag:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3000"
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: mongodb-secret
                  key: uri
            - name: RAG_LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openai-secret
                  key: apiKey
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: RAG_VECTOR_DB_URL
              value: "http://qdrant-service:6333"
            - name: RAG_VECTOR_DB_TYPE
              value: "qdrant"
            - name: RAG_VECTOR_INDEX_NAME
              value: "logiCore_embeddings"
            - name: RAG_VECTOR_DIMENSIONS
              value: "1536"
            - name: RAG_EMBEDDING_MODEL
              value: "text-embedding-3-small"
            - name: RAG_LLM_PROVIDER
              value: "openai"
            - name: RAG_LLM_MODEL
              value: "gpt-4-turbo"
            - name: RAG_REDIS_URL
              value: "redis://redis-service:6379"
            - name: RAG_CACHE_TTL_SECONDS
              value: "300"
            - name: RAG_TESSERACT_LANG
              value: "ara+eng"
            - name: RAG_CROSS_ENCODER_MODEL
              value: "cross-encoder/ms-marco-MiniLM-L-6-v2"
            - name: RAG_ASSEMBLYAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: assemblyai-secret
                  key: apiKey
                  optional: true
            - name: REPORTS_DIR
              value: "/app/reports"
            - name: UPLOADS_DIR
              value: "/app/uploads/incidents"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          volumeMounts:
            - name: reports
              mountPath: /app/reports
            - name: uploads
              mountPath: /app/uploads/incidents
      volumes:
        - name: reports
          persistentVolumeClaim:
            claimName: reports-pvc
        - name: uploads
          persistentVolumeClaim:
            claimName: uploads-pvc
